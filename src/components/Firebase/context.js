// import React from 'react';

// //createContext creates 2 components 
// //-provider - provide firebase instance once at top level of react component tree
// //-consumer - retrieve firebase instance if it is needed in react component
// const FirebaseContext = React.createContext(null);

// export default FirebaseContext;